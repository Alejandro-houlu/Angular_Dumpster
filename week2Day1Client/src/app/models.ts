export interface BookSummary{
    bookId: String
    title: String
}

export interface BookDetails{
    bookId:string
    title: string
    authors: string
    description: string
    rating: string
    imageUrl: string
}